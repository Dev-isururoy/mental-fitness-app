import React from 'react';

const Footer = () => {
    return (
        <footer>
            <p>&copy; {new Date().getFullYear()} Mental Fitness App</p>
            {/* Add additional footer content as needed */}
        </footer>
    );
};

export default Footer;
